let arr = [1, 2, 3, 4, 5];
//console.log(arr[4]);


//........................// Array methods
// Array methods are functions that operate on arrays, allowing you to manipulate and interact with the data

/*
const cities = ["Manchester", "Liverpool"];
const newLength = cities.push("Bristol");
console.log(cities); // [ "Manchester", "Liverpool", "Bristol" ]
console.log(newLength); // 3
console.log(cities[2]); // "Bristol"
console.log(cities.length); // 3 */



/*
const cities = ["Manchester", "Liverpool"];
cities.pop();
console.log(cities); // [ "Manchester" ]
console.log(cities.length); // 1
*/



/*
function double(number) {
  return number * 2;
}
const numbers = [5, 2, 7, 6];
const doubled = numbers.map(double);
console.log(numbers); // [ 5, 2, 7, 6 ]
// The original array remains unchanged
console.log(doubled); // [ 10, 4, 14, 12 ]
*/


/*
// The `filter` method creates a new array with all elements that pass the test implemented by the provided function.
// It does not modify the original array.
function isLong(city){
  return city.length > 8;
}

const cities = ["London", "Liverpool", "Totnes dyrfyt", "Edinburgh"];
const longer = cities.filter(isLong);
console.log(longer); // [ "Liverpool", "Edinburgh" ]
// The original array remains unchanged*/


/*
function isLong(village) {
  return village.length >7;
}

 const villages = ["Nahar", "kosli" , "kannina" , "dadri" , "nangal padthani"]
  const longer =villages.filter(isLong);
  console.log(longer); */
  
/*
const gao = ["Nahar", "kosli" , "kannina" , ["dadri" , "nangalpadthani"]];
const random = gao[2][0];
  console.log(random);

*/

/*
let fruits = ["apple", "banana", "cherry"];
console.log(fruits);
// Adding an element to the end of the array
fruits.push("orange");
console.log(fruits);

const birds = ["sparrow " , "pecock" , "pigon"];
for(const bird of birds) {
  console.log(bird);
  
} */


  /*
let numbers = [1, 9, 3, 4, 5];
  const min = Math.min(...numbers);
  console.log(min); */

  
  let numbers = [1, 9, 3, 4, 5];
  let min1 = Infinity;
  let min2 = infinity;
  for(let i=0; i<numbers.length; i++ ) {
    if()
  }

